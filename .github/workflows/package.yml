name: Package

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "pyproject.toml"
      - "README.md"
      - "uv.lock"
      - "src/**"
      - ".github/workflows/package.yml"
  workflow_dispatch:

concurrency:
  group: package-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  build-and-install:
    name: Build and install package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.13"
          enable-cache: true

      - name: Verify lockfile
        run: uv lock --check

      - name: Build wheel and source distribution
        run: uv build --out-dir dist

      - name: Verify build artifacts
        shell: bash
        run: |
          shopt -s nullglob
          wheels=(dist/*.whl)
          sdists=(dist/*.tar.gz)
          test "${#wheels[@]}" -eq 1
          test "${#sdists[@]}" -eq 1
          python -m zipfile -l "${wheels[0]}" | grep 'tarkka/'

      - name: Install wheel into clean environment
        run: |
          uv venv --python 3.13 .package-wheel-venv
          uv pip install --python .package-wheel-venv/bin/python dist/*.whl

      - name: Smoke-test installed wheel and CLI
        run: |
          .package-wheel-venv/bin/python -c "import tarkka"
          .package-wheel-venv/bin/tarkka --help

      - name: Install source distribution into clean environment
        run: |
          uv venv --python 3.13 .package-sdist-venv
          uv pip install --python .package-sdist-venv/bin/python dist/*.tar.gz

      - name: Smoke-test installed source distribution and CLI
        run: |
          .package-sdist-venv/bin/python -c "import tarkka"
          .package-sdist-venv/bin/tarkka --help

      - name: Retain distributions
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: python-distributions
          path: dist/
          if-no-files-found: error
          retention-days: 14
